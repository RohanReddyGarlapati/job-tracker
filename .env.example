# Copy to .env. For local dev this points at a SQLite file.
# For production (e.g. Vercel) swap this for a Postgres/Turso connection string —
# see the "Deploying to Vercel" section of the README.
DATABASE_URL="file:./dev.db"
